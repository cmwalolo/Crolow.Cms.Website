@using Crolow.Cms.Core.HtmlHelpers.Dictionary
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{
    var selection = Model.Root().Children().Where(x => x.IsVisible());
}
<header>
    @await Component.InvokeAsync("navigation", new { content = UmbracoContext.PublishedRequest.PublishedContent })
</header>